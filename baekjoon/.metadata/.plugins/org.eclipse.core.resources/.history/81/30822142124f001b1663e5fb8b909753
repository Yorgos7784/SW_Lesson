package ave_per;

import java.util.Scanner;

public class Main {

	public static void main(String[] args) {
	
		Scanner s = new Scanner(System.in);
		double stuscore[][] = new double[s.nextInt()][];
		for (int i = 0; i < stuscore.length; i++) {
			for (int j = 0; j < s.nextInt(); j++) {
				stuscore[i][j] = s.nextInt();
			}
		}
		
		for (int i = 0; i < stuscore.length; i++) {
			for (int j = 0; j < stuscore[i].length; j++) {
				System.out.printf("arr[%d][%d] : %d", i, j, stuscore[i][j]);
			}
		}

	}

}
